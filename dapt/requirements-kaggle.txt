# Extra deps for Kaggle notebooks: torch/transformers/pandas/numpy/tqdm are
# already preinstalled there, so only what's missing goes here.
# Usage: pip install -r dapt/requirements-kaggle.txt
sentencepiece>=0.1.99   # required by the mT5 tokenizer
protobuf>=3.20          # required to load the mT5 SentencePiece model

# For build_lexicon.py (data-driven lexicon):
wordfreq[cjk]           # keyness/weirdness scoring; [cjk] adds mecab-python3 +
                        # ipadic (ja), mecab-ko-dic (ko), jieba (zh) tokenizers
fasttext-wheel          # seed expansion via nearest neighbours (--expand)
